import styled from 'styled-components/native';

import { metrics } from '../styles';

export const Wrapper = styled.View`
  flex: 1;
`;

export const RightHeaderWrapper = styled.TouchableOpacity`
  padding: 0 ${metrics.spacing}px;
`;
